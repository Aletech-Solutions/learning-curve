# XandLearning Environment Variables
# Copy this file to .env and adjust values as needed

# Ports (using uncommon ports to avoid conflicts)
FRONTEND_PORT=4173
BACKEND_PORT=4174
MONGODB_PORT=27117

# Environment
NODE_ENV=development

# MongoDB (only change if using external MongoDB)
# MONGODB_URI=mongodb://mongodb:27117/xandlearning

